# Hoist all packages to node_modules root — required for NestJS decorator
# metadata and DI to work correctly with pnpm's virtual store.
shamefully-hoist=true

# Prevent install failures from unresolved peer dependency warnings.
strict-peer-dependencies=false
