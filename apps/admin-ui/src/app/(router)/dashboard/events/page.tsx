// All events product
// add Export CSV
// its similar to products with 7 table columns with Image, Title, Price, Stock, Start, End, Shop Name.
// it should fetch /admin/get-all-events?page=${page}&limit=${limit}
