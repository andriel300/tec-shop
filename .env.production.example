# Production Environment Template
# SECURITY NOTE: Use environment variables or secrets management in production

# Google OAuth - Use production OAuth app
GOOGLE_CLIENT_ID="production-google-client-id"
GOOGLE_CLIENT_SECRET="production-google-client-secret"
GOOGLE_CALLBACK_URL="https://yourdomain.com/api/auth/google/callback"

# Redis - Use production Redis instance
REDIS_URL="production-redis-connection-string"

# Database URLs - Use production databases
AUTH_SERVICE_DB_URL="production-auth-database-url"
USER_SERVICE_DB_URL="production-user-database-url"

# SMTP - Use production email service
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=production-smtp-username
SMTP_PASS=production-smtp-password
SMTP_FROM=noreply@yourdomain.com
SMTP_SECURE=true

# Service URLs - Use production domains
FRONTEND_URL="https://yourdomain.com"
BACKEND_URL="https://api.yourdomain.com"
# Note: NEXT_PUBLIC_API_URL not needed in production (uses relative /api)

# Security Keys - Use strong, unique secrets for production
JWT_SECRET="production-jwt-secret-minimum-64-characters-for-security"

# Microservice Configuration - Use production service discovery
AUTH_SERVICE_HOST=auth-service
AUTH_SERVICE_PORT=6001
USER_SERVICE_HOST=user-service
USER_SERVICE_PORT=6002

# Additional Production Settings
NODE_ENV=production
PORT=8080