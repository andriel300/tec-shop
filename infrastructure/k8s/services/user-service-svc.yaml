apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: tec-shop
  labels:
    app: user-service
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: tec-shop
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: tcp
      port: 6002
      targetPort: 6002
      protocol: TCP
