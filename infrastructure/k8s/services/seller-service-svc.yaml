apiVersion: v1
kind: Service
metadata:
  name: seller-service
  namespace: tec-shop
  labels:
    app: seller-service
    app.kubernetes.io/name: seller-service
    app.kubernetes.io/part-of: tec-shop
spec:
  type: ClusterIP
  selector:
    app: seller-service
  ports:
    - name: tcp
      port: 6003
      targetPort: 6003
      protocol: TCP
