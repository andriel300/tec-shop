apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: tec-shop
  labels:
    app: order-service
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: tec-shop
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: tcp
      port: 6005
      targetPort: 6005
      protocol: TCP
