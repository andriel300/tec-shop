apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: tec-shop
  labels:
    app: admin-service
    app.kubernetes.io/name: admin-service
    app.kubernetes.io/part-of: tec-shop
spec:
  type: ClusterIP
  selector:
    app: admin-service
  ports:
    - name: tcp
      port: 6006
      targetPort: 6006
      protocol: TCP
