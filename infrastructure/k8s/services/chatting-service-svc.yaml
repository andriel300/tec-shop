apiVersion: v1
kind: Service
metadata:
  name: chatting-service
  namespace: tec-shop
  labels:
    app: chatting-service
    app.kubernetes.io/name: chatting-service
    app.kubernetes.io/part-of: tec-shop
spec:
  type: ClusterIP
  selector:
    app: chatting-service
  ports:
    - name: tcp
      port: 6007
      targetPort: 6007
      protocol: TCP
    # TODO: Add WebSocket port if separate from TCP port
