apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: tec-shop
  labels:
    app: auth-service
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: tec-shop
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: tcp
      port: 6001
      targetPort: 6001
      protocol: TCP
