apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatting-service
  namespace: tec-shop
  labels:
    app: chatting-service
    app.kubernetes.io/name: chatting-service
    app.kubernetes.io/part-of: tec-shop
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chatting-service
  template:
    metadata:
      labels:
        app: chatting-service
        app.kubernetes.io/name: chatting-service
    spec:
      containers:
        - name: chatting-service
          # TODO: Replace with your container registry image path
          image: your-registry/tec-shop-chatting-service:latest
          ports:
            - containerPort: 6007
              protocol: TCP
          envFrom:
            - configMapRef:
                name: tec-shop-config
            - secretRef:
                name: tec-shop-secrets
          env:
            - name: PORT
              value: "6007"
            - name: NODE_ENV
              value: "production"
          # TODO: Tune resource limits based on load testing results
          # TODO: WebSocket services may need sticky sessions - configure ingress accordingly
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 6007
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 6007
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
