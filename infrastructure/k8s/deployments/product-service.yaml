apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: tec-shop
  labels:
    app: product-service
    app.kubernetes.io/name: product-service
    app.kubernetes.io/part-of: tec-shop
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
        app.kubernetes.io/name: product-service
    spec:
      containers:
        - name: product-service
          # TODO: Replace with your container registry image path
          image: your-registry/tec-shop-product-service:latest
          ports:
            - containerPort: 6004
              protocol: TCP
          envFrom:
            - configMapRef:
                name: tec-shop-config
            - secretRef:
                name: tec-shop-secrets
          env:
            - name: PORT
              value: "6004"
            - name: NODE_ENV
              value: "production"
          # TODO: Tune resource limits based on load testing results
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "750m"
              memory: "384Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 6004
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 6004
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
