apiVersion: apps/v1
kind: Deployment
metadata:
  name: logger-service
  namespace: tec-shop
  labels:
    app: logger-service
    app.kubernetes.io/name: logger-service
    app.kubernetes.io/part-of: tec-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logger-service
  template:
    metadata:
      labels:
        app: logger-service
        app.kubernetes.io/name: logger-service
    spec:
      containers:
        - name: logger-service
          # TODO: Replace with your container registry image path
          image: your-registry/tec-shop-logger-service:latest
          ports:
            - containerPort: 6008
              protocol: TCP
          envFrom:
            - configMapRef:
                name: tec-shop-config
            - secretRef:
                name: tec-shop-secrets
          env:
            - name: PORT
              value: "6008"
            - name: NODE_ENV
              value: "production"
          # TODO: Tune resource limits based on log volume
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 6008
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 6008
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
