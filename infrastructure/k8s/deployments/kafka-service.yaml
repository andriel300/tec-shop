apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-service
  namespace: tec-shop
  labels:
    app: kafka-service
    app.kubernetes.io/name: kafka-service
    app.kubernetes.io/part-of: tec-shop
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kafka-service
  template:
    metadata:
      labels:
        app: kafka-service
        app.kubernetes.io/name: kafka-service
    spec:
      containers:
        - name: kafka-service
          # TODO: Replace with your container registry image path
          image: your-registry/tec-shop-kafka-service:latest
          ports:
            - containerPort: 6009
              protocol: TCP
          envFrom:
            - configMapRef:
                name: tec-shop-config
            - secretRef:
                name: tec-shop-secrets
          env:
            - name: PORT
              value: "6009"
            - name: NODE_ENV
              value: "production"
          # TODO: Tune resource limits based on analytics throughput
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 6009
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 6009
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
