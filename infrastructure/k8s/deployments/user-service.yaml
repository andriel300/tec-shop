apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: tec-shop
  labels:
    app: user-service
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: tec-shop
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        app.kubernetes.io/name: user-service
    spec:
      containers:
        - name: user-service
          # TODO: Replace with your container registry image path
          image: your-registry/tec-shop-user-service:latest
          ports:
            - containerPort: 6002
              protocol: TCP
          envFrom:
            - configMapRef:
                name: tec-shop-config
            - secretRef:
                name: tec-shop-secrets
          env:
            - name: PORT
              value: "6002"
            - name: NODE_ENV
              value: "production"
          # TODO: Tune resource limits based on load testing results
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 6002
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 6002
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
